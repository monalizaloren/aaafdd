import React from 'react';
import { View, Text, Image, TouchableOpacity, StyleSheet, Linking } from 'react-native';
import { Feather } from '@expo/vector-icons'; // Importe o ícone de seta

const HarryPotter = ({ navigation }) => {
  // Função para lidar com o botão de voltar
  const handleGoBack = () => {
//UTILIZE O CÓDIGO 'navigation.goBack();' PARA VOLTAR A TELA INICIAL
    ???
  };

  // Função para abrir o link externo
  const openHBOmaxLink = () => {
//COLOQUE O LINK AQUI
    Linking.openURL('???');
  };

  return (
    <View style={styles.container}>
      <Image
        source={require('../assets/h.jpg')}
        style={styles.image}
      />
      <View style={styles.header}>
        //PROGRAME QUE QUANDO O BOTÃO FOR PRESSIONADO, A FUNÇÃO 'HANDLEGOBACK' IRÁ SER CHAMADA
        ???
          <Feather name="arrow-left" size={24} color="#FFD700" /> {/* Ícone de seta com cor dourada */}
        </TouchableOpacity>
        <Text style={styles.title}>Harry Potter</Text>
      </View>
      <Text style={styles.description}>Descrição</Text>
      <Text style={styles.sinopse}>
        A história mágica de Harry Potter, um jovem bruxo que descobre seu
        destino em Hogwarts e enfrenta desafios incríveis ao lado de seus
        amigos.
      </Text>
        ////PROGRAME QUE QUANDO O BOTÃO FOR PRESSIONADO, A FUNÇÃO 'OPENHBOMAXLINK' IRÁ SER CHAMADA
      ???
        <Text style={styles.linkButtonText}>Assista no HBO Max</Text>
      </TouchableOpacity>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: '#331e11', // Marrom inspirado na paleta de cores do Harry Potter
    paddingVertical: 16,
  },
  image: {
    width: '100%',
    height: 300,
  },
  header: {
    backgroundColor: 'rgba(0, 0, 0, 0.6)',
    width: '100%',
    padding: 10,
    flexDirection: 'row',
    alignItems: 'center',
  },
  title: {
    fontSize: 24,
    color: '#FFD700', // Amarelo dourado inspirado no tema Harry Potter
    marginLeft: 10,
  },
  description: {
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 20,
    color: 'white',
  },
  sinopse: {
    fontSize: 16,
    marginHorizontal: 20,
    textAlign: 'center',
    color: 'white',
  },
  linkButton: {
    backgroundColor: '#FFD700', // Amarelo dourado para o botão
    padding: 16,
    borderRadius: 8,
    marginTop: 20,
  },
  linkButtonText: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#331e11', // Marrom para o texto do botão
    textAlign: 'center',
  },
});

export default HarryPotter;
