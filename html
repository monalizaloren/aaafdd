/
            let meteor_arr = Object.keys(this.state.meteors).map(meteor_date => {
                return this.state.meteors[meteor_date]
            })
            /* Aqui, você está usando o método concat para combinar os arrays individuais em meteor_arr em um único array chamado meteors. Isso é feito para que você possa realizar operações subsequentes em todos os dados dos meteoros em um único array.*/
            let meteors = [].concat.apply([], meteor_arr);
            meteors.forEach(function (element) {
                let diameter = (element.estimated_diameter.kilometers.estimated_diameter_min + element.estimated_diameter.kilometers.estimated_diameter_max) / 2
                let threatScore = (diameter / element.close_approach_data[0].miss_distance.kilometers) * 1000000000
                element.threat_score = threatScore;
            });
            /* Acima, você está iterando sobre cada elemento no array meteors usando o método forEach. Para cada elemento (que representa os dados de um meteoro), você está calculando a média do diâmetro estimado do meteoro e, em seguida, calculando uma pontuação de ameaça com base no diâmetro médio e na distância mais próxima da Terra do meteoro. Essa pontuação de ameaça é atribuída à propriedade threat_score do elemento.*/
            meteors.sort(function (a, b) {
                return b.threat_score - a.threat_score
            })

            /*o método sort para classificar os elementos no array meteors com base em suas pontuações de ameaça. Os elementos com pontuações de ameaça mais altas serão classificados primeiro. */
            meteors = meteors.slice(0, 5)
            /*  você está usando o método slice para pegar apenas os primeiros 5 elementos do array meteors, que são os 5 meteoros mais ameaçadores com base nas pontuações de ameaça calculadas anteriormente*/
